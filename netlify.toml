[build]
  publish = "public"
  command = "hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.147.1"
  HUGO_ENV     = "production"

[[redirects]]
  from = "/admin"
  to   = "/admin/index.html"
  status = 200

[dev]
  command = "hugo server -D"
